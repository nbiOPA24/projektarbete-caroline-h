class Program
{
    static void Main(string[] args)
    {
        // Skapa rumobjekt
        var singleRoom = new SingleRoom(101);
        var doubleRoom = new DoubleRoom(201);
        var suite = new Suite(301);

        // Checka in gäster en åt gången
        CheckInGuest(singleRoom, doubleRoom, suite);
        CheckInGuest(singleRoom, doubleRoom, suite);
        CheckInGuest(singleRoom, doubleRoom, suite);
    }
